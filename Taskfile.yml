# https://taskfile.dev
---
version: "3"

tasks:
  stow:
    deps:
      - stow-zsh

  stow-zsh:
    cmd: stow -t "$HOME" -R zsh

  setup:
    cmds:
      - ./setup/install-packages.sh
    preconditions:
      - sh: test "$(whoami)" = "root"
        msg: Task 'setup' must be run as root!
